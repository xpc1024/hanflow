"""Core data contracts — the foundation of Hanflow.

All other modules depend only on ``hanflow.core`` plus their own boundary.
This package re-exports the public contract surface for convenience (errors
are added in Task 3, result/state/dsl/expr/context in later tasks).
"""
